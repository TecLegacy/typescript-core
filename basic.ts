// Types

const userName = '2';

const obj = {
  sad: 'mae',
};

[1, 2, 3].forEach(v => console.log(v));

const keshav = (a: number, b: number) => {
  return a + b;
};

const x = keshav(10, 20);

console.log(x);
